# 🎯 RojarHat - দ্রুত সেটআপ গাইড (বাংলা)

আপনার সাইট এখনই লাইভ করতে এই স্টেপগুলো ফলো করুন। মাত্র ৫ মিনিট লাগবে!

---

## 📦 কী কী তৈরি হয়েছে?

✅ **সম্পূর্ণ ফাইল তৈরি হয়েছে:**
1. `supabase-setup.sql` - সম্পূর্ণ ডাটাবেস সেটআপ (১৮টি প্রোডাক্ট + ৫টি ক্যাটাগরি)
2. `SUPABASE_GUIDE.md` - বিস্তারিত সেটআপ গাইড (ইংরেজি + বাংলা)
3. পেজে fallback data যোগ করা হয়েছে (Supabase না থাকলেও সাইট চলবে)

---

## 🚀 এখন কী করবেন? (Step-by-Step)

### 1️⃣ Supabase Project তৈরি করুন
- যান: https://supabase.com
- Sign in করুন (GitHub দিয়ে সহজ)
- "New Project" ক্লিক করুন
- নাম দিন: `RojarHat`
- Region: `Southeast Asia (Singapore)`
- Password দিন এবং সেভ করুন
- ২-৩ মিনিট অপেক্ষা করুন

### 2️⃣ API Keys কপি করুন
- Settings > API তে যান
- দুইটা জিনিস কপি করুন:
  - Project URL
  - anon public key

### 3️⃣ .env.local ফাইল তৈরি/আপডেট করুন
প্রজেক্টের root folder-এ:

```env
NEXT_PUBLIC_SUPABASE_URL=আপনার_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=আপনার_anon_key
```

### 4️⃣ Database Setup করুন (গুরুত্বপূর্ণ!)
1. Supabase Dashboard > **SQL Editor** এ যান
2. "New query" ক্লিক করুন
3. `supabase-setup.sql` ফাইলটি খুলুন
4. **পুরো ফাইল কপি করে পেস্ট করুন**
5. "Run" বাটন চাপুন
6. Success দেখলে আপনি সফল! ✅

এই SQL স্ক্রিপ্ট করবে:
- 4টি table তৈরি করবে
- 5টি category যোগ করবে
- 18টি product যোগ করবে
- Stock management function সেটআপ করবে

### 5️⃣ Dev Server Restart করুন
Terminal-এ:
```bash
# Ctrl+C চেপে server বন্ধ করুন
pnpm dev
```

### 6️⃣ টেস্ট করুন
- `http://localhost:3000` খুলুন
- Products দেখা যাচ্ছে? ✅
- Shop page এ categories আছে? ✅
- Cart এ add হচ্ছে? ✅

---

## ✅ Verify করুন (ডাটা ঠিক মতো আছে কিনা)

Supabase Dashboard-এ:
1. **Table Editor** > `products` - দেখবেন ১৮টি row
2. **Table Editor** > `categories` - দেখবেন ৫টি row
3. **Table Editor** > `orders` - চেকআউট করার পর এখানে দেখা যাবে

---

## 🛒 এখন কী করতে পারেন?

### ✨ নতুন Product যোগ করুন:
1. Supabase > Table Editor > `products`
2. "Insert" > "Insert row" ক্লিক করুন
3. নতুন product এর তথ্য দিন
4. Save করুন - সাথে সাথে সাইটে দেখা যাবে!

### 📊 Orders দেখুন:
1. Supabase > Table Editor > `orders`
2. সব অর্ডার এখানে দেখা যাবে
3. Status change করতে পারবেন (pending → processing → delivered)

### 🖼️ Product Images যোগ করুন:
- `public/images/` folder-এ নতুন image রাখুন
- Supabase-এ product-এর `image_url` field-এ image path দিন
- Format: `/images/your-image-name.webp`

---

## 🎉 সাইট এখন সম্পূর্ণ Ready!

আপনার সাইট এখন:
- ✅ Products দেখাচ্ছে (Supabase থেকে)
- ✅ Categories কাজ করছে
- ✅ Cart/Checkout কাজ করছে
- ✅ Stock automatically কমে যাবে order দিলে
- ✅ Fallback আছে (Supabase down থাকলেও চলবে)

---

## 📖 বিস্তারিত জানতে:
- `SUPABASE_GUIDE.md` পড়ুন (সম্পূর্ণ গাইড)
- `supabase-setup.sql` দেখুন (সব SQL code)

---

## 🆘 সমস্যা?

### Products দেখা যাচ্ছে না?
- `.env.local` চেক করুন (keys ঠিক আছে?)
- Dev server restart করেছেন?
- Supabase SQL query ঠিকভাবে রান হয়েছে?

### Error দেখা যাচ্ছে?
- Browser Console (F12) খুলে error message দেখুন
- Supabase Dashboard > Table Editor এ গিয়ে data চেক করুন

---

**🎊 সাইট চালু করার জন্য অভিনন্দন! এখন বিক্রি শুরু করুন!** 🎊
