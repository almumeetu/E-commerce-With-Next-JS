# 🚀 Friends Gallery Backend Setup Guide (Supabase)

এই গাইডটি অনুসরণ করে আপনি আপনার ইকমার্স প্রজেক্টের ব্যাকএন্ড (Database, API, Storage) সম্পূর্ণভাবে সেটআপ করতে পারবেন।

---

## 📋 ধাপ ১: Supabase প্রজেক্ট তৈরি

১. [Supabase Dashboard](https://supabase.com/dashboard) এ যান এবং লগইন করুন।
২. **"New Project"** বাটনে ক্লিক করুন।
৩. প্রজেক্টের নাম দিন (যেমন: `Friends Gallery`)।
৪. ডাটাবেস পাসওয়ার্ড দিন (এটি মনে রাখুন)।
৫. Region হিসেবে `Southeast Asia (Singapore)` সিলেক্ট করুন (বাংলাদেশের কাছের সার্ভার)।
৬. **"Create new project"** দিন এবং ২-৩ মিনিট অপেক্ষা করুন।

---

## 🔑 ধাপ ২: API Keys সংগ্রহ

১. প্রজেক্ট ড্যাশবোর্ড থেকে বামদিকের মেনুতে **Settings (⚙️)** > **API** এ যান।
২. **Project URL** এবং **anon public key** কপি করুন।

---

## 📝 ধাপ ৩: Environment Variables সেটআপ

আপনার প্রজেক্টের রুট ফোল্ডারে `.env.local` ফাইলটি খুলুন (না থাকলে তৈরি করুন) এবং নিচের তথ্যগুলো বসান:

```env
NEXT_PUBLIC_SUPABASE_URL=আপনার_প্রজেক্ট_ইউআরএল
NEXT_PUBLIC_SUPABASE_ANON_KEY=আপনার_anon_public_key
```

> **সতর্কতা:** `.env.local` ফাইলটি কখনোই গিটহাবে আপলোড করবেন না।

---

## 🗄️ ধাপ ৪: ডাটাবেস সেটআপ (সবচেয়ে গুরুত্বপূর্ণ)

আমরা একটি মাত্র SQL ফাইলের মাধ্যমে পুরো ডাটাবেস, টেবিল, পারমিশন এবং ডেমো ডাটা তৈরি করবো।

১. Supabase ড্যাশবোর্ডে বামদিকের মেনু থেকে **SQL Editor** এ যান।
২. **New query** বাটনে ক্লিক করুন।
৩. আপনার প্রজেক্টের `supabase-setup.sql` ফাইলটি খুলুন এবং **পুরো কোড কপি করুন**।
৪. Supabase SQL Editor-এ কোডটি **পেস্ট** করুন এবং **Run** বাটনে ক্লিক করুন।
৫. নিচে "Success" মেসেজ দেখলে বুঝবেন কাজ হয়েছে।

> **দ্রষ্টব্য:** এই স্ক্রিপ্টটি `products`, `orders`, `customers` টেবিল তৈরি করবে এবং কিছু ডেমো ডাটা ইনসার্ট করবে। এতে পারমিশন সমস্যা সমাধানের কোডও যুক্ত আছে।

---

## 🖼️ ধাপ ৫: স্টোরেজ (Storage) সেটআপ

প্রোডাক্টের ছবি আপলোড করার জন্য স্টোরেজ বাকেট তৈরি করতে হবে।

১. Supabase ড্যাশবোর্ডে **Storage** মেনুতে যান।
২. **"New Bucket"** বাটনে ক্লিক করুন।
৩. বাকেটের নাম দিন: `products`
৪. **"Public bucket"** অপশনটি **অন (ON)** করে দিন।
৫. **"Save"** করুন।

> এখন আপনি ড্যাশবোর্ড থেকে বা আপনার অ্যাপ থেকে এই বাকেটে ছবি আপলোড করতে পারবেন।

---

## ✅ ধাপ ৬: টেস্ট করা

১. টার্মিনালে `pnpm dev` কমান্ড দিয়ে প্রজেক্ট রান করুন।
২. ব্রাউজারে `http://localhost:5173` (বা আপনার পোর্ট) এ যান।
৩. হোমপেজে প্রোডাক্ট দেখা যাচ্ছে কিনা চেক করুন।
৪. Cart এ প্রোডাক্ট অ্যাড করে চেকআউট করার চেষ্টা করুন।

---

## 🛠️ সাধারণ সমস্যা ও সমাধান

**সমস্যা:** ডাটাবেস থেকে ডাটা আসছে না।
**সমাধান:**
- `.env.local` ফাইলে সঠিক URL এবং Key আছে কিনা চেক করুন।
- Supabase SQL Editor এ `supabase-setup.sql` সঠিকভাবে রান হয়েছে কিনা নিশ্চিত হোন।
- ব্রাউজারের কনসোল (F12) চেক করুন কোনো লাল এরর আছে কিনা।

**সমস্যা:** পারমিশন ডিনাইড (Permission Denied) এরর।
**সমাধান:**
- `supabase-setup.sql` ফাইলটি আবার রান করুন। এতে পারমিশন ফিক্স করার কোড আছে।

---

## 📚 প্রয়োজনীয় ফাইলসমূহ

আপনার প্রজেক্টে এখন দুটি প্রধান ফাইল আছে ব্যাকএন্ডের জন্য:
১. `supabase-setup.sql`: ডাটাবেস স্কিমা এবং সেটআপ স্ক্রিপ্ট।
২. `BACKEND_SETUP_GUIDE.md`: এই গাইড ফাইলটি।

বাকি সব অপ্রয়োজনীয় সেটআপ ফাইল ডিলিট করে দেওয়া হয়েছে প্রজেক্ট ক্লিন রাখার জন্য।
